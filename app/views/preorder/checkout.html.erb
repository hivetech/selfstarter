<div class="gray_background">
  <div class="wrapper clearfix">
    <div class="main_content <%= "payment_options" if Settings.use_payment_options %>">
      <h3>Support</h3>
      <p>
      You can contribute to unide by two means. Head out soon to our <a
        href="http://indiegogo.com">Indiegogo campaign</a> or join us building
      Unide on <a href=https://github.com/hivetech>Github</a>. Either ways you
      can subscribe to our mailing list, and
      we will keep you updated.
        <br />
        <br />
        <% if Settings.use_payment_options %>
          Select a payment option and enter your email address below.
        <% else %>
          Mailing list
        <% end %>

      </p>
      <%= form_tag "/preorder/prefill", :id => "checkout" do %>
        <%= render 'preorder/payment_options' %>
        <div class="checkout_controls">
          <div class="checkout_controls_wrapper">
            <div class="checkout_controls_inner">
              <%= email_field_tag "email", nil, :placeholder => "coming !", :required => "required", :id => "email" %>
              <%= hidden_field_tag "preorder", true %>
              <%= hidden_field_tag "quantity", params[:quantity] %>
              <%#<%= submit_tag "Subscribe", :class => "blue_button disabled", :id => "amazon_button" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
    <%= render 'preorder/checkout/sidebar' %>
  </div>
</div>
